using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FogGenerator : MonoBehaviour
{
    [SerializeField] private GameObject fogClouds;

    // Start is called before the first frame update
    void Start()
    {

    }
}
